> Windows 系统下使用 VS Code 学习 Rust no_std 开发 ESP32

**1.Rust 工具链安装**

通过官网 https://rustup.rs 安装`rustup`，然后用它安装 Rust 工具链。安装完成后，在终端中检查一下：

```powershell
rustup --version
rustc --version
cargo --version
```

**2.VS Code 扩展安装**

在 VS Code 中进行 Rust 开发，建议安装以下扩展：

| 扩展名              | 功能                        |
| ---------------- | ------------------------- |
| rust-analyzer    | Rust 语言服务器，提升代码补全、跳转、错误提示 |
| Even Better TOML | 为 Cargo.toml 提供更好的语法高亮和补全 |
| Error Lens       | 直接在编辑器里显示编译错误位置           |

**3.ESP32 工具链安装**

ESP32 Rust 开发通常需要以下工具：

- espup：一键安装 ESP32 Rust 开发环境
- espflash：固件烧录工具，支持烧录和串口监视
- cargo-generate：快速生成项目模板

安装命令如下：

```powershell
cargo install espup
espup install
cargo install espflash
cargo install cargo-generate
```

检查是否安装成功：

```powershell
espup --version
espflash --version
cargo generate --version
```
对于`no_std`模式的项目，ESP 官方现在更推荐使用`esp-generate`作为项目生成工具。它能更快地生成合适的工程模板，减少手动配环境的工作量。

安装命令：

```powershell
cargo install esp-generate --locked
```

安装完成后，可以先检查是否可用：

```powershell
esp-generate --help
```
