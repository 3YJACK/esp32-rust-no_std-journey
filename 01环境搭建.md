>  在window系统下，使用VS code学习Rust no_std 开发ESP32 

**1.Rust工具链**

 通过网站 https://rustup.rs 下载rust工具链管理器`rustup`，并通过`rustup`安装好rust工具链。

**2.VScode扩展**

要在vscode中进行rust开发，还需要添加以下扩展:
| 扩展名 | 功能 |
|---|---
rust-analyzer | rust语言服务器，进行rust开发的必备扩展
Even Better TOML | Cargo.toml 配置文件的语法高亮和智能提示
Error Lens | 把错误信息直接显示在代码行旁边

**3.ESP32工具链**

espup : 一键安装 ESP32 Rust 全套环境。

espflash : 固件烧录工具, 附带串口监视功能。

cargo-generate : 快速生成项目模板, 用官方模板创建项目，省去手动配环境的麻烦。

```
// 工具下载命令
cargo install espup
espup install
cargo install espflash
cargo install cargo-generate

// 检查是否安装成功
espup --version
espflash --version
cargo generate --version
```

对`no_std`模式的模板生成工具，esp官方现推荐使用`esp-generate`，使用下列命令安装： 

```
cargo install esp-generate --locked
```
